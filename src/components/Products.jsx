const Products = () => {
    // const [products, setProducts] = useState([]);

    // const getProducts = async () => {

    // }

    return (
        <section>
            Hello Products
        </section>
    )
}

export default Products;
